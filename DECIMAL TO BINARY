//Program to convert decimal to binary number

void toBinary(int N)
{
    int bin=0,rem;
    int i=1;
    while(N!=0)
    {
        rem=N%2;
        N/=2;
        bin+=rem*i;
        i*=10;
    }
    cout<<bin;
        
}
